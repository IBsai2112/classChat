'use strict';

var GetIntrinsic = require('get-intrinsic');

var $ArrayBuffer = GetIntrinsic('%ArrayBuffer%', true);
var $Uint8Array = GetIntrinsic('%Uint8Array%', true);

var IsDetachedBuffer = require('es-abstract/2024/IsDetachedBuffer');
var IsSharedArrayBuffer = require('es-abstract/2024/IsSharedArrayBuffer');
var max = require('es-abstract/2024/max');
var min = require('es-abstract/2024/min');
var SpeciesConstructor = require('es-abstract/2024/SpeciesConstructor');
var ToIntegerOrInfinity = require('es-abstract/2024/ToIntegerOrInfinity');

var arrayBufferByteLength = require('array-buffer-byte-length');
var isArrayBuffer = require('is-array-buffer');
var $TypeError = require('es-errors/type');

module.exports = function slice(start, end) {
	var O = this; 

	if (!isArrayBuffer(O) || IsSharedArrayBuffer(O) || IsDetachedBuffer(O)) {
		throw new $TypeError('receiver must be a non-detached, non-shared ArrayBuffer'); 
	}

	var len = arrayBufferByteLength(O); 

	var relativeStart = ToIntegerOrInfinity(start); 

	var first;
	if (relativeStart === -Infinity) {
		first = 0; 
	} else if (relativeStart < 0) {
		first = max(len + relativeStart, 0); 
	} else {
		first = min(relativeStart, len); 
	}

	var relativeEnd = typeof end === 'undefined' ? len : ToIntegerOrInfinity(end); 

	var final;
	if (relativeEnd === -Infinity) {
		final = 0; 
	} else if (relativeEnd < 0) {
		final = max(len + relativeEnd, 0); 
	} else {
		final = min(relativeEnd, len); 
	}

	var newLen = max(final - first, 0); 

	var Ctor = SpeciesConstructor(O, $ArrayBuffer); 

	var new$ = new Ctor(newLen); 

	if (!isArrayBuffer(new$) || IsSharedArrayBuffer(new$) || IsDetachedBuffer(new$)) {
		throw new $TypeError('Species constructor must produce a non-detached, non-shared Array Buffer'); 
	}

	if (new$ === O) {
		throw new $TypeError('new ArrayBuffer should not have been the same as the receiver'); 
	}

	if (arrayBufferByteLength(new$) < newLen) {
		throw new $TypeError('new ArrayBuffer\'s byteLength must be at least the requested length'); 
	}

	if (IsDetachedBuffer(O)) {
		throw new $TypeError('receiver became an detached ArrayBuffer'); 
	}

	
	var sourceArr = new $Uint8Array(O);
	var destArr = new $Uint8Array(new$);
	for (var i = start, ii = 0; i < end; i++, ii++) {
		destArr[ii] = sourceArr[i];
	}

	return new$; 
};
