'use strict';

var callBound = require('call-bound');


var $deref =  (callBound('WeakRef.prototype.deref', true));


module.exports = typeof WeakRef === 'undefined'
	? function isWeakRef(_value) { 
		return false;
	}
	: function isWeakRef(value) {
		if (!value || typeof value !== 'object') {
			return false;
		}
		try {
			
			$deref(value);
			return true;
		} catch (e) {
			return false;
		}
	};
